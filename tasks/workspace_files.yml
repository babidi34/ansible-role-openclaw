---
- name: Ensure workspace files exist
  ansible.builtin.file:
    path: "{{ openclaw_workspace_dir }}/{{ item }}"
    state: touch
    owner: "{{ openclaw_user }}"
    group: "{{ openclaw_group }}"
    mode: "0640"
  loop: "{{ openclaw_workspace_files }}"
  when: openclaw_workspace_init | bool
