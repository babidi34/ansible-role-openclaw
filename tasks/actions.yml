---
- name: Ensure actions allowlist/denylist are lists
  ansible.builtin.assert:
    that:
      - openclaw_actions_allowlist is iterable
      - openclaw_actions_denylist is iterable
    fail_msg: "Set allowlist/denylist as lists"
