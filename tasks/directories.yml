---
- name: Create Moltbot directories
  ansible.builtin.file:
    path: "{{ item }}"
    state: directory
    owner: "{{ moltbot_user }}"
    group: "{{ moltbot_group }}"
    mode: "0750"
  loop:
    - "{{ moltbot_config_dir }}"
    - "{{ moltbot_data_dir }}"
    - "{{ moltbot_log_dir }}"
    - "{{ moltbot_workspace_dir }}"
    - "{{ moltbot_upstream_dir }}"
    - "{{ moltbot_upstream_credentials_dir }}"
    - "{{ moltbot_upstream_agents_dir }}"
    - "{{ moltbot_tmp_dir }}"
